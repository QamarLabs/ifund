namespace QamarLabs.Microservices.FundEntities.Extensions
{
    public class MongoDbContextExtensions
    {
        /*
        public async Task InsertRecord<T>(this MongoDbContext context, T record, string collectionName) where T : class
        {
            using (var collection = context.GetCollection<T>(collectionName))
            {

            }
        }
        */
    }
}
